import * as util from './util.mjs';

const numeros = [1, 2, 3, 4, 5];

console.log(`Soma do array: ${util.somaArray(numeros)}`);
console.log(`Maior valor: ${util.maiorValor(numeros)}`);
console.log(`Menor valor: ${util.menorValor(numeros)}`);
